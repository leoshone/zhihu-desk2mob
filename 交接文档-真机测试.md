# 交接文档：zhihu-desk2mob 返回键关评论 · 真机测试与修复

> 交接时间：2026-09-04 16:06
> 交接目标：在另一台电脑上连接 Android 手机（Kiwi 浏览器），真机复现「系统返回键无法关闭专栏页评论区反而退回首页」的 bug，取证确认根因后修复脚本（v0.7.8）。
> 当前版本：**v0.7.7**（已推送 GitHub main，`f716507`；取证脚本在 `d779fb0`）

---

## 1. 项目一句话

油猴脚本 `zhihu-desk2mob.user.js`：把知乎桌面版网页在 Kiwi「桌面版网站」模式下收进手机宽度（原理 `html{width:393px;zoom:980/393}` 抵消 Chromium 的 980 视口缩放）。当前在修的 bug：**系统返回键应关闭评论区并留在本页，实际却直接退回首页**。

## 2. 问题状态速览（别重复走过的路）

| 版本 | 方案 | 结果 |
|---|---|---|
| ≤0.7.4 | 被动检测 fixed 浮层 → 压缓冲历史 | 问题页（fixed 弹层）✅；专栏页 ❌（无浮层可检测） |
| 0.7.5 | 点击拦截：识别评论按钮点击时压缓冲 | ❌ `\b评论\b` 是死代码（`\b` 对中文永不成立） |
| 0.7.6 | 修掉 `\b`、支持 svg 图标、冒泡 6 层 | ❌ 用户仍报不行 |
| 0.7.7 | 滚动过深自动压缓冲（滚过 (文档高−一屏)×0.5 → pushState） | 本机模拟 ✅，**用户真机仍 ❌（待真机取证）** |

**已取证的真实专栏页事实**（`zhuanlan.zhihu.com/p/2074785936261505339`，未登录状态，`测试脚本/forensics_real_column.py`）：
- 专栏页是 **window 滚动**（docScrollHeight=24528 / clientHeight=2130，无任何内部滚动容器）——「内部容器滚动」怀疑已被证伪
- 评论区**未登录也常驻内联渲染**：`div.Comments-container` 801×5939@y17561
- 评论按钮：`button.Button.ContentItem-action`，文本=`"\u200b675 条评论"`（**开头有零宽空格**）
- 未登录访问会弹**登录弹层**（fixed Modal），它会触发脚本浮层检测路径，**污染测试结果**（本机"返回留本页"PASS 可能是登录弹层关掉的功劳，不是滚动缓冲）
- **用户真机是登录态** → 没有登录弹层 → 与本机环境的关键差异

**头号待验证怀疑**（真机取证目标）：
1. 登录态专栏页的评论区形态与未登录不同（可能点「675 条评论」按钮才展开？展开方式是滚动定位还是别的？）
2. 登录态下用户点评论按钮时 `isCommentTrigger` 是否命中（零宽空格 `\u200b` 是否干扰？v0.7.6 的 `indexOf('评论')` 理论上能过，但没在登录态真机上验过）
3. v0.7.7 滚动压缓冲在真机 Kiwi 上是否触发（滚动事件是否被页面吃掉 / 阈值是否合理）
4. Kiwi 老内核（Chromium ~100?）对 `history.pushState`/`popstate`/`scrollTo behavior:smooth` 的兼容性

## 3. 需要转移的文件

**最小集（拷这些就够）：**
```
D:\AiSpaces\Code\zhihu-desk2mob\          ← 整个目录最好全拷
├─ zhihu-desk2mob.user.js                  ★ 主脚本 v0.7.7（被测对象）
├─ zhihu-desk2mob.diag.user.js             ★ 诊断版（屏上调试面板，diagnostic 分支也有）
├─ README.md / 安装说明.md                  （装 Tampermonkey 用）
└─ 测试脚本\
   ├─ lib.py                               ★ Playwright 桌面模式配置（DESKTOP_MODE）
   ├─ forensics_real_column.py             ★ 真实专栏页取证脚本（改 TARGET URL 可换页）
   ├─ test_real_comment_back.py            ★ 真实站点问题页端到端（绕风控模板）
   ├─ test_zhuanlan_inline_back.py         专栏复刻页 e2e（点击+滚动双场景）
   ├─ testpage_zhuanlan_inline.html        专栏复刻页
   ├─ demo_simulation.py                   四幕演示（了解测试方法用）
   └─ test_spa_back.py / test_modal.py / test_xclose_node.py   回归套件
```
（或者直接 `git clone https://github.com/leoshone/zhihu-desk2mob.git`，以上全部都在仓库里，含 `.workbuddy/memory/` 的完整过程笔记）

## 4. 新电脑环境准备

1. **Python 3.13+** + `pip install playwright` + `playwright install chromium`
2. **adb**（关键，本机也没有）：下载 [Android platform-tools](https://developer.android.com/tools/releases/platform-tools)，解压后把 adb.exe 路径加进 PATH（或记住绝对路径）
3. 手机端：设置 → 关于手机 → 连点「版本号」7 次开开发者选项 → 开「USB 调试」
4. 手机装 Kiwi 浏览器 + Tampermonkey，装 v0.7.7 脚本（GitHub raw 地址，见 §6），并**登录知乎账号**（复现用户真实环境）

## 5. 真机测试 SOP（Kiwi 远程调试）

Kiwi 的招牌功能就是支持 chrome://inspect 远程调试，流程：

```bash
# 1) 手机 USB 连电脑（选「文件传输」模式，弹窗授权 USB 调试）
adb devices                      # 确认设备列出
# 2) 手机 Kiwi 打开目标专栏页 https://zhuanlan.zhihu.com/p/2074785936261505339
#    Kiwi 菜单里开「开发者工具」→ 会显示 WebView 调试开关
# 3) 电脑上查 Kiwi 的 devtools socket：
adb shell cat /proc/net/unix | grep devtools
#    输出类似 @webview_devtools_remote_<pid>  记下这个名字
# 4) 端口转发（把手机 socket 映射到本机 9222）：
adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>
# 5) 验证：浏览器打开 http://127.0.0.1:9222/json  应返回页面 JSON 列表
```

取证脚本骨架（Python + Playwright）：
```python
from playwright.sync_api import sync_playwright
with sync_playwright() as p:
    browser = p.chromium.connect_over_cdp("http://127.0.0.1:9222")
    ctx = browser.contexts[0]
    pg = ctx.pages[0]           # 手机上正开着的那个页面
    # —— 取证（只读，不动用户页面）——
    print(pg.evaluate("() => ({url: location.href, winY: scrollY, state: history.state, len: history.length})"))
    # 滚动结构：window 还能滚多少
    print(pg.evaluate("() => ({sh: document.documentElement.scrollHeight, ch: document.documentElement.clientHeight})"))
    # —— 真实系统返回键 ——
    import subprocess; subprocess.run(["adb", "shell", "input", "keyevent", "4"])
```

**取证清单（按序执行，每步记录）：**
1. 初始态：`{url, winY, history.state, history.length}`
2. 用户手动滚到评论区（或脚本 `pg.evaluate("window.scrollTo(0, 99999)")`）后：winY 变了吗？`state.zfModal` 出现了吗？（v0.7.7 滚动压缓冲是否触发 → 看 Tampermonkey console「弹层：滚动过深，压入缓冲历史」）
3. **真实返回键** `adb shell input keyevent 4` 后：URL 变了吗？（退回首页 = bug 复现）`state` 变成什么？
4. 重复 3 次，确认稳定复现
5. 若复现：在 console 查 `window.__zfDiag`（若装诊断版）或查 Tampermonkey 日志，确认滚动监听是否触发过

**对照实验（定位用）：**
- 关掉 v0.7.7 只装诊断版 → 看滚动时 ticks/滚动标志是否变化
- 问题页（/question/xxx）重复同样操作 → 问题页应该一直是好的，作对照组

## 6. 关键链接与 SHA

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/leoshone/zhihu-desk2mob |
| 当前 main HEAD | `d779fb0`（v0.7.7 = `f716507`） |
| v0.7.7 安装链（raw，免 CDN 缓存） | `https://raw.githubusercontent.com/leoshone/zhihu-desk2mob/f716507204ef699774c49233f1603f1264af556c/zhihu-desk2mob.user.js` |
| 诊断版（diagnostic 分支） | `https://raw.githubusercontent.com/leoshone/zhihu-desk2mob/80f62ec0d15b4e7357066126bb44876f18499bb7/zhihu-desk2mob.diag.user.js` |
| ⚠️ CDN 缓存坑 | jsDelivr 和 raw 的 @main 都有缓存，**验证新版必须用带 commit SHA 的 raw 地址** |

## 7. 修 v0.7.8 时的注意事项（血泪教训浓缩）

- **`\b` 对中文永远不成立**：`/\b评论\b/` 是死代码。中文匹配用 `indexOf` 或无边界正则（v0.7.6 教训，node 一行可验证）
- **复刻页测试通过 ≠ 真机通过**：页面生命周期、登录态、弹层干扰都会改变行为路径。本机未登录有登录弹层污染，真机登录态才是目标环境
- **「角标在」≠「功能在」**：SPA 路由切换会重置 JS 上下文，window 属性会被清掉；取证先确认功能真的初始化了
- **初始化别写 `if (!document.body) return`**：SPA 定时炸弹，要定时重试直到 body 就绪
- **MutationObserver 回调里禁止依赖布局的祖先爬升**：插入瞬间未布局，几何判据全不可信（v0.6.0 白屏事故）
- **关弹层优先点知乎原生关闭按钮**（语义匹配 → 触发 React 状态复位），别直接 display:none
- **弹层关闭按钮千万别钉成 fixed**：会被困在弹层堆叠上下文里，z-index 再高也点不到
- 修完先 node 一行单测判据 → 本地回归（test_spa_back/test_modal/test_xclose_node/test_zhuanlan_inline_back）→ 真实站点 e2e（test_real_comment_back，注意登录弹层干扰）→ **最后真机**
- 完整历史教训在 `.workbuddy/memory/MEMORY.md` 和 `2026-09-02.md` / `2026-09-04.md`

## 8. 预期成果

1. 真机复现 bug（登录态专栏页：滚到评论区 → 系统返回 → 退回首页）
2. 取证确定真实根因（滚动监听没触发？触发了但 Kiwi popstate 行为不同？登录态评论区形态不同？）
3. 修复 → v0.7.8，按 §7 顺序验证后推 GitHub，更新 README changelog + CDN SHA
