# 知乎桌面版 → 手机宽度适配：制作与测试全记录

> 交付物：`zhihu-desk2mob.user.js` v0.3.0（另含诊断工具 `zhihu-diag.user.js`）
> 目标环境：Android Kiwi 浏览器（Chromium 内核），勾选「桌面版网站」访问 `www.zhihu.com`
> 本文记录从拿到参考脚本到交付的完整过程，包括每一步的实测数据、踩过的坑、以及最终验证结果。所有数据均可复现，测试脚本在 `测试脚本/` 目录。

---

## 目录

- [0. 结论先行](#0-结论先行)
- [1. 需求与约束](#1-需求与约束)
- [2. 原理：Kiwi 桌面模式到底做了什么](#2-原理kiwi-桌面模式到底做了什么)
- [3. 现场勘查：知乎现在长什么样](#3-现场勘查知乎现在长什么样)
- [4. 参考脚本 v3.1 的失效诊断](#4-参考脚本-v31-的失效诊断)
- [5. v4 的设计与实现](#5-v4-的设计与实现)
- [6. 测试体系](#6-测试体系)
- [7. 测试结果](#7-测试结果)
- [8. 过程中发现并修复的缺陷](#8-过程中发现并修复的缺陷)
- [9. 工程踩坑记录（与脚本无关）](#9-工程踩坑记录与脚本无关)
- [10. 已知限制与未覆盖场景](#10-已知限制与未覆盖场景)
- [11. 附录：文件清单与复现步骤](#11-附录文件清单与复现步骤)
- [12. 真机反馈的处理方式](#12-真机反馈的处理方式)
- [13. 真正的元凶：脚本自己把正文压塌了](#13-真正的元凶脚本自己把正文压塌了)

---

## 0. 结论先行

| 项目 | 结果 |
|---|---|
| 参考脚本 v3.1 是否可用 | **否，实测完全不生效**（数据与不装脚本完全一致） |
| v3.1 失效根因 | `document-start` 阶段 `document.head` 为 `null`，`appendChild` 抛错导致整个 IIFE 中断 |
| v0.1 桌面模式效果 | `zoom=2.4936`、布局宽 `393px`、横向溢出 `0`、超宽元素 `0` 个 |
| v0.1 移动模式副作用 | 无。且顺带修好了知乎自己的移动端溢出 bug（639px → 0） |
| **v0.3 修复：脚本自己把正文压塌** | 专栏页正文从 694px 被压到 24px。根因是 flex 收缩压力全落在唯一被清零 min-width 的 item 上 → 改为容器换行，正文恢复 345px |
| 未验证项 | 真机观感（模拟环境无法替代肉眼，见第 10 节） |

---

## 1. 需求与约束

用户想在手机 Kiwi 浏览器里访问**知乎桌面版**网页，要求按参考脚本的思路把它适配成手机宽度，并且**做完后仔细测试**。

拆解出三条硬约束：

1. **必须是桌面版网页**，不是知乎自家的移动版（`m.zhihu.com`）。理由很实际：移动版砍掉了大量功能，且强制诱导下载 App。
2. **Kiwi 的「桌面版网站」模式是前提**。这个模式会让 Chromium 伪装成桌面浏览器，拿到桌面版 HTML，但也带来了一套特殊的视口行为——这是整个任务的核心难点。
3. **要能真机可用**，不是"看起来能跑"。所以测试必须是量化可复现的，不能只靠"我写了应该没问题"。

---

## 2. 原理：Kiwi 桌面模式到底做了什么

### 2.1 视口是怎么被撑开的

Pixel 7 的屏幕是 393×852 设备独立像素。正常移动模式下，浏览器把 layout viewport 设成 393，1 CSS px = 1 设备独立 px，一切正常。

勾选「桌面版网站」后，Chromium 为了"装作自己是桌面浏览器"，会做一件事：

```
layout viewport 强行 = 980 CSS px（Chromium 的常量）
屏幕只有 393 物理 px → 整页缩小到 393/980 ≈ 0.4 倍投屏
```

后果很直观：知乎正文字号 15px，屏幕上实际只有 6px；页面宽度 1032px，得左右拖着看。

这里有个关键点容易被忽略：**缩小的不是 CSS，是渲染结果**。知乎的 CSS 里写的 `width: 1032px` 依然是 1032px，浏览器只是在最后投屏时整体缩了 0.4 倍。所以单纯改 CSS 宽度没用——你改到 393px，投屏后只剩 157px，更小了。

### 2.2 zoom 抵消法

正确做法是"抵消浏览器的缩小"，而不是改布局：

```css
html { width: 393px; zoom: 980/393; }
```

推导一遍：

```
布局宽度 = 393px
zoom 放大 2.494 倍 → 393 × 2.494 = 980px（正好填满 layout viewport）
浏览器再整体缩 0.4 倍投屏 → 980 × 0.4 = 393px
```

净效果：**1 CSS px = 1 设备独立 px**。字号回到正常大小，页面宽度刚好一屏，而且知乎的桌面版布局结构完整保留（这点很重要，桌面版的信息密度比移动版高得多）。

### 2.3 zoom 的三个陷阱

`zoom` 看起来是一行 CSS 的事，但它会污染整个坐标系。这三个坑 v3 全踩了：

**陷阱一：`vh` / `vw` 会放大 2.49 倍**

这两个单位基于 layout viewport，而 layout viewport 被撑到了 980×2130。于是 `100vh` = 2130px，在 393 宽的布局里等于 5 个屏幕高——任何用 `vh` 做高度的弹窗会直接顶飞出屏幕。

对策：把所有 `vh`/`vw` 折算成基准坐标系下的 px。

**陷阱二：`getBoundingClientRect()` 返回缩放后的坐标**

这是最隐蔽的一个。元素实际宽 393px，zoom 2.494 之后 `getBoundingClientRect().width` 返回 980。如果拿它做溢出检测，会误判成"严重溢出"，然后疯狂压缩元素——页面会塌掉。

对策：**溢出检测必须用 `offsetWidth`**，它不受 zoom 影响，返回的是布局坐标系下的真实宽度。

**陷阱三：`document-start` 时 `document.head` 是 `null`**

`@run-at document-start` 意味着脚本在文档创建的最早阶段执行，此时 `<head>` 还没被创建。`document.head.appendChild(style)` 直接抛 `Cannot read properties of null`。

**这个陷阱直接判了 v3 死刑**，见第 4 节。

---

## 3. 现场勘查：知乎现在长什么样

写适配脚本最忌讳的就是对着几个月前的 DOM 结构写选择器。所以动手前先做了一次完整的现场取证（`测试脚本/forensics.py`）。

### 3.1 探测手段

用 Playwright + Chromium 无头浏览器，桌面 UA 访问 `https://www.zhihu.com/question/19550225`，等 6 秒让 React 完成水合，然后统计。

> 补充：知乎有反爬。`curl` 直连返回 403，加 `-L` 会跳到 `signin`。必须用真实浏览器访问。实测 `question/19550225` 虽返回 403 状态码但 SSR 内容完整（文本长度 6200～6800 字符），`explore` 返回 200，两者都可用于布局测试。

### 3.2 页面规模

首屏（未触发懒加载）实测：

```
DOM 元素总数:        1110
emotion 原子类元素:   184
样式表:               84 张
CSS 规则:           7208 条
含 vw/vh 的规则:      28 条
```

滚动触发懒加载后，DOM 元素会增长到 6000+。这个数量级直接决定了后面扫描器的实现方式（见 5.3.2 和 8.1）。

知乎已经全面迁移到 emotion 原子化 CSS——每个元素挂一个哈希类名（`.css-51utkw`、`.css-iilrph`），样式全在运行时注入的 `<style>` 里。84 张样式表、7208 条规则，几乎全是哈希类名。

### 3.3 v3 依赖的选择器存活情况

这是最关键的一次取证。逐个统计 v3 CSS 里用到的语义 class 在真实页面上的命中数：

| 选择器 | 命中数 | 说明 | 状态 |
|---|---:|---|---|
| `.AppHeader-inner` | 0 | v3 顶栏内层 | **死代码** |
| `.SearchBar-container` | 0 | v3 搜索框容器 | **死代码** |
| `.Question-main` | 0 | v3 问题主区 | **死代码** |
| `.Topstory-container` | 0 | v3 首页容器 | **死代码** |
| `.Topstory-mainColumn` | 0 | v3 首页主列 | **死代码** |
| `.ColumnSideBar` | 0 | v3 专栏侧栏 | **死代码** |
| `.GlobalSideBar` | 0 | v3 全局侧栏 | **死代码** |
| `.Post-NormalMain` | 0 | v3 文章主体 | **死代码** |
| `.AppHeader` | 1 | 顶栏 | 存活 |
| `.SearchBar` | 1 | 搜索框 | 存活 |
| `.Question-mainColumn` | 1 | 主列 | 存活 |
| `.Question-sideColumn` | 1 | 右侧栏 | 存活 |
| `.QuestionHeader-title` | 2 | 问题标题 | 存活 |
| `.RichText` / `.RichContent` | 各 3 | 富文本容器 | 存活 |
| `header.AppHeader nav` | 1 | 顶栏导航 | 存活 |

**17 个选择器里 8 个已经完全消失，命中率 53%。**

这个数据直接决定了 v4 的架构：

> **不能再靠 CSS 选择器追页面结构。** 知乎用 emotion 原子类，改版时哈希类名随时会变。主力必须是"按计算后的实际宽度收拾"，CSS 只用来写那些不容易失效的规则（语义 class + 结构选择器）。

### 3.4 真正的溢出元凶

统计所有 `min-width > 400px` 的元素，只有 3 个，但就是这 3 个撑爆了整页：

```
min-width=1175px  实测宽 1175px  <header> .AppHeader css-iilrph
min-width=1032px  实测宽 1032px  <div>    .QuestionHeader
min-width= 694px  实测宽  694px  <div>    .QuestionHeader-main QuestionHeader-footer-main
```

顶栏 1175px、问题头部 1032px、主列 694px。注意前两个都超过了 layout viewport 的 980px，所以即使不 zoom，页面也已经横向溢出 52px（1032 - 980）。

基线测量（不装任何脚本，桌面模式）：

```
zoom=1  layoutW=980  cssW=980  scrollW=1032  clientW=980
横向溢出=52px   超宽元素=13 个（问题页）/ 35 个（发现页）
```

---

## 4. 参考脚本 v3.1 的失效诊断

### 4.1 现象：数据和没装脚本完全一致

把 v3.1 注入模拟环境，和不装脚本的基线逐项对比：

| 指标 | 不装脚本 | v3.1 | 差异 |
|---|---:|---:|---|
| zoom | 1 | **1** | 无 |
| 布局宽度 | 980px | **980px** | 无 |
| 文档滚动宽 | 1032px | **1032px** | 无 |
| 横向溢出 | 52px | **52px** | 无 |
| 超宽元素数 | 13 / 35 | **13 / 35** | 无 |

**v3.1 对页面造成的改变是零。** 不是"效果不好"，是"完全没生效"。

### 4.2 根因：head 是 null

页面错误日志抓到了铁证：

```
Cannot read properties of null (reading 'appendChild')
```

定位到 v3.1 的第 77 行：

```js
var style = document.createElement('style');
style.id = 'zhihu-mobile-fix';
style.textContent = css;
document.head.appendChild(style);   // ← document-start 阶段 head 为 null，这里抛错
```

因为 `@run-at document-start`，脚本执行时 `document.head` 还是 `null`。这一抛的后果比看起来严重得多：

1. `injectCSS()` 抛错 → 异常向上传播
2. 调用它的 `injectCSS()` 在第 81 行，是**裸调用**，没有 try/catch
3. 异常冲出 IIFE 顶层 → **整个脚本剩余部分全部不执行**
4. 第 86 行之后的 `onBodyReady()`（阶段二的 JS 扫尾）**一次都没跑过**

最讽刺的是第 29 行那句日志：

```js
console.log(TAG, 'sw=' + sw, 'vw=' + vw, 'needZoom=' + needZoom, 'zoom=' + zoom.toFixed(2), ...);
```

它在 `injectCSS()` **之前**，所以能正常打印 `zoom=2.49`。如果你只看控制台，会以为脚本工作正常——实际上它算出了正确的 zoom 值，然后死在了下一行，CSS 从未进过 DOM。

这是个典型的"看起来在调试，实际上被日志误导"的场景。也说明了一条经验：**验证脚本是否生效，必须看页面的实际计算结果（`getComputedStyle`），不能看脚本自己打了什么日志。**

### 4.3 v3.1 的次要问题

即使修好注入，v3 还有三个问题等着：

| 问题 | 说明 |
|---|---|
| 选择器大量失效 | 53% 命中率，见 3.3 |
| `getBoundingClientRect()` 测宽 | 在 zoom 下返回放大 2.49 倍的值，溢出检测会误判 |
| 扫描深度截断 | `walk(el, depth)` 里 `depth > 12` 就停，且 `shouldSkip` 把 `SPAN`/`A`/`IMG` 等整类元素跳过——漏掉大量需要处理的节点 |
| `svg` 被设成块级居中 | 第 52 行 `img,figure,video,iframe,svg,canvas{display:block;margin:10px auto}` 会把所有内联图标变成块级并居中，顶栏会散架 |

---

## 5. v4 的设计与实现

### 5.0 开发迭代过程

动手写代码前，先花了大半时间做环境搭建和现场取证——事后看这个顺序是对的，如果一上来就照着 v3 的思路改，会写出第二版同样无效的东西。

> **关于版本号的说明**：本仓库对外从 `0.1.0` 开始。开发过程中为了和参考脚本 v3.1 区分，内部沿用了 `v4.0` / `v4.1` 作为开发代号——下表以及第 8、11 章中出现的 v4.x 均为内部代号，与对外发布版本无关。

| 阶段 | 做了什么 | 得到的关键结论 |
|---|---|---|
| ① 理解需求 | 读 v3.1 源码，理清"zoom 抵消缩放"的思路 | 思路本身是对的，问题出在落地 |
| ② 搭测试环境 | Playwright 模拟 Pixel 7，复现 Kiwi 桌面模式的 980/393 视口行为 | 必须能测才有资格谈"做完了" |
| ③ 打通访问 | 试 `curl` 被反爬 403，改用真实浏览器 | 403 但 SSR 内容完整，可用于布局测试 |
| ④ 现场取证 | 统计 DOM 规模、选择器存活率、硬编码宽度 | **v3 选择器 53% 命中**；溢出元凶是 3 个 min-width |
| ⑤ 写首个版本（代号 v4.0） | 健壮注入 + 结构选择器 + JS 宽度扫描 | 首次跑通，但 `count=203` |
| ⑥ 修扫描 | 去截断、读写分离、多轮收敛、补 flex/负 margin | 203 → 0 |
| ⑦ 修拼写错误 | pageerror 揪出 `fixHeader` 拼写错误 | 监听全部没注册，之前是"假装在跑" |
| ⑧ v4.0 回归 | 3 配置 × 2 页面 × 2 模式全量对比 | 桌面 0 溢出、移动无副作用，看似完成 |
| ⑨ 旋屏自检 | 加测旋屏与站内跳转 | **发现 zoom 永久失效 + vh/vw 无法还原** |
| ⑩ 修复并定稿（对外 v0.1） | 设备短边锚定 + 三重兜底 + CSS 变量 | 四种状态 zoom 稳定 2.494 |
| ⑪ 最终回归 | 全量重跑确认无退化 | v0.1 全绿 |
| ⑫ 用户反馈专栏页 | 正文被右侧栏挤成窄条 | v0.1 漏了两栏布局这个场景 |
| ⑬ 复刻测试页 | 专栏页撞登录墙 + IP 风控，改造复刻页验证 | 启发式识别不依赖类名，成立 |
| ⑭ 修 grid 列宽 | 侧栏隐藏后主列只剩 49px | **grid 列宽是硬分配的**，必须改容器 |
| ⑮ 子代理读图复核 | 第一轮确认 flex、但拍漏了 grid | 重截取证，两种变体视觉均通过 |

⑧ 之后本来可以交付了——桌面模式 0 溢出、移动模式无副作用，数据很漂亮。是"旋屏自检"这个自发的加测把两个真 bug 挖了出来。这大概就是"仔细测一下"和"跑一遍看看"的区别。

### 5.1 分层策略

针对"知乎会改版"这个既定事实，v4 采用三层防御：

```
第一层：CSS 规则（快，但会随改版失效）
   ↓ 只写语义 class + 结构选择器，不碰哈希类名
第二层：JS 按实际宽度扫描（慢，但永远有效）
   ↓ 读 offsetWidth → 超了才处理 → 多轮收敛
第三层：MutationObserver 持续维护
   ↓ 增量扫新增节点，大块变更才全量重扫
```

核心思想：**不猜页面结构，量页面实际宽度**。CSS 选择器会随改版失效，但"这个元素宽 1175px 而屏幕只有 393px"这个事实永远不会失效。

### 5.2 模块清单

| # | 模块 | 职责 |
|---|---|---|
| 1 | `injectStyle()` | 健壮的样式注入，head 为 null 时多重回退 |
| 2 | `screenWidth()` / `compute()` | 环境测量，算出 zoom 与基准宽 |
| 3 | `buildCSS()` | 生成核心 CSS |
| 4 | `markScrollables()` / `applyFix()` / `fixWidths()` | JS 宽度修复主力 |
| 5 | `fixFixedLayers()` | 处理 fixed/sticky 悬浮层 |
| 6 | `fitHeader()` | 顶栏自适应裁剪 |
| 6.5 | `fitColumns()` | 两栏布局识别与侧栏处理（专栏页等）|
| 6.6 | `fixFlexRows()` | flex/grid 行崩塌修复（正文被压成窄条）|
| 7 | `setVUnitVars()` / `fixViewportUnits()` | vh/vw 修正 |
| 8 | `wrapTables()` | 表格横向滚动包装 |
| 9 | `safe()` / `applyAll()` | 错误隔离与编排 |
| 10 | `boot()` | 启动、监听、自检入口 |

### 5.3 关键实现细节

#### 5.3.1 `injectStyle()`：修好 v3 的致命伤

```js
function injectStyle(cssText, id) {
  function apply() {
    var parent = document.head || document.documentElement;   // 双回退
    if (!parent) return false;
    var old = document.getElementById(id);
    if (old) old.parentNode.removeChild(old);                 // 幂等，重复调用不会累积
    var st = document.createElement('style');
    st.id = id;
    st.textContent = cssText;
    parent.appendChild(st);
    return true;
  }
  if (apply()) return;
  // 回退一：等 DOM 长出 head
  var mo = new MutationObserver(function () { if (apply()) mo.disconnect(); });
  mo.observe(document, { childList: true, subtree: true });
  // 回退二：1 秒内无论如何落地
  var timer = setInterval(function () {
    if (apply() || Date.now() - t0 > 1000) { clearInterval(timer); mo.disconnect(); }
  }, 10);
}
```

三重保障：先试 `head`，不行试 `documentElement`，还不行就 MutationObserver 监听 + 1 秒轮询兜底。

#### 5.3.2 `fixWidths()`：读写分离 + 多轮收敛

这是把超宽元素从 203 个降到 0 的核心。两个关键设计：

**读写分离**。如果边读 `offsetWidth` 边写 `style`，每次写都会让布局失效，下次读就要重新计算——6000 个元素的页面上会触发 6000 次 reflow，卡死。所以：

```js
// ① 整批读（只触发一次 reflow）
for (var i = 0; i < n; i++) widths[i] = nodes[i].offsetWidth;

// ② 只对超宽的调 getComputedStyle（很贵，能省则省）
for (var i = 0; i < n; i++) {
  if (widths[i] <= base + 1) continue;
  if (el.hasAttribute('data-zskip')) continue;
  batch.push([el, getComputedStyle(el)]);
}

// ③ 整批写
for (var i = 0; i < batch.length; i++) applyFix(batch[i][0], batch[i][1], base);
```

**多轮收敛**。父元素收缩后子元素才会跟着收缩，一轮不够。跑 3 轮，任一轮没有超宽元素就提前退出。

`applyFix()` 处理六种撑宽手法：

```js
el.style.setProperty('max-width', '100%', 'important');
el.style.setProperty('box-sizing', 'border-box', 'important');
if (parseFloat(cs.minWidth) > base)  → min-width: 0
if (/px$/.test(cs.width) && 超宽)     → width: 100%      // 显式 px 宽度
if (/px$/.test(cs.flexBasis) && 超宽) → flex-basis: auto // flex 基准
if (cs.flexShrink === '0')            → flex-shrink: 1   // 禁止收缩
if (ml < 0 || mr < 0)                 → margin: 0        // 负 margin 通栏
```

前两个是常规操作，后四个是实测中发现的漏网之鱼——知乎大量使用 `flex-basis` 和负 margin 做通栏效果，只清 `min-width` 完全不够。

#### 5.3.3 `markScrollables()`：有意横向滚动的白名单

代码块、表格、图片轮播**本来就该横向滑动**。如果把它们也压缩，代码会挤成一坨完全没法看。

```js
if (cs.overflowX === 'auto' || cs.overflowX === 'scroll') {
  el.setAttribute('data-zskip', '1');
  var sub = el.querySelectorAll('*');
  for (var j = 0; j < sub.length; j++) sub[j].setAttribute('data-zskip', '1');
}
```

给整个子树打标记。之后 `fixWidths` 里只需 `hasAttribute('data-zskip')` 一次 O(1) 判断，不用每次往上遍历祖先。

#### 5.3.4 `fixViewportUnits()`：用 CSS 变量而非写死数值

第一版是把 `100vh` 直接替换成算好的 px：

```js
// 初版（有缺陷）
nv = val.replace(reVh, (m, num) => (parseFloat(num) * vhPx).toFixed(3) + 'px');
```

问题：**文本替换只能做一次**。旋屏后 `vhPx` 变了，但原来的 `100vh` 已经变成 `854px` 了，无法还原。

改成替换成 CSS 变量表达式：

```js
nv = val.replace(reVh, (m, num) => 'calc(' + num + ' * var(--zf-vh))');
```

配合每次 `applyAll()` 都刷新的变量声明：

```js
':root{ --zf-vw:3.9300px; --zf-vh:8.5400px; }'
```

这样旋屏时只需更新变量值，不用重扫 7208 条规则。同时加了 `val.indexOf('--zf-v') >= 0` 的幂等判断，避免重复替换把 `calc()` 套成俄罗斯套娃。

实测命中 28 条规则、共修正 33 处属性值（一条规则可能有多个属性用到 `vw`/`vh`）。

#### 5.3.5 `safe()`：错误隔离

v3 的教训是"一个点炸掉整条链"。v4 里所有阶段调用都包一层：

```js
function safe(name, fn) {
  try { return fn(); }
  catch (e) { log(name + ' 失败', e && e.message); return undefined; }
}
```

这样即使某个模块因为知乎改版而抛错，其他模块照常工作，最坏情况是效果打折而不是全盘失效。

#### 5.3.6 `fitHeader()`：顶栏裁剪

393px 装不下 logo + 7 个导航项 + 搜索框 + 登录按钮。做法是让顶栏容器 `flex-wrap: wrap`，然后按实测宽度从前往后累加，放不下的整段收起：

```js
var used = 0, cut = false;
for (var i = 0; i < items.length; i++) {
  used += items[i].offsetWidth || 0;
  if (!cut && used > avail) {
    for (var j = i; j < items.length; j++) items[j].style.display = 'none';
    cut = true;
  }
}
```

注意每轮开始前先 `removeProperty('display')` 重置，否则上一轮隐藏的项不会回来（旋屏变宽时会出现"导航项再也回不来"的 bug）。

#### 5.3.7 为什么不碰 `svg`

v3 的规则里有 `img,figure,video,iframe,svg,canvas{display:block!important;margin:10px auto!important}`。这会把**所有内联图标**变成块级元素并水平居中——顶栏的 logo、搜索图标、头像会全部错位。

v4 的媒体规则只作用于 `.RichText`/`.RichContent`/`.ztext` 内容容器内，且明确排除 `svg`：

```css
.RichText img, .RichContent img, .ztext img, figure img,
.RichText video, ..., .RichText iframe, ..., .RichText canvas {
  max-width:100%!important; height:auto!important; display:block!important; margin:12px auto!important;
}
```

### 5.4 CSS 里的两个细节

**用 `overflow-x: clip` 而不是 `hidden`**。`hidden` 会创建滚动容器，导致 `position: sticky` 的顶栏失效（sticky 依赖最近滚动祖先）。`clip` 只裁剪不创建滚动上下文。

**弹窗不用 `vh`**。`.Modal`/`.Drawer` 这类元素如果写 `max-height: 85vh`，在 zoom 下会变成 1810px（2130 × 0.85），直接顶飞。v4 完全不用 `vh`，改用 JS 算好的 px。

### 5.5 `fitColumns()`：不依赖类名的两栏识别

专栏页的右侧栏是 v0.1 漏掉的场景。加这个功能时有个明确的约束：**不能再堆语义 class**。第 3.3 节已经证明知乎的选择器命中率只有 47%，靠类名识别侧栏，等于排着队等下一次改版失效。

所以 `fitColumns()` 走的是**结构启发式**——只问"谁在左边、谁的文本多"，不问"你叫什么"。

**识别流程：**

```
1. 找候选容器：offsetWidth >= 屏幕宽 × 0.6，子元素 2~8 个
2. 筛子元素：块级、可见、offsetWidth >= 60、非 fixed/absolute
3. 按 top 分组（相差 ≤ 40px 视为同一行），取元素最多的那组
4. 组内按 left 排序 → 最左的当主列，最右的当侧栏
5. 三重校验：
     · 侧栏宽度 >= 容器 20%      （小挂件不是侧栏）
     · 主列文本量 >= 侧栏文本量  （别把辅助栏当主列）
     · 主列文本量 >= 200 字符    （主列得真有内容）
6. 处理：
     · hide   → 侧栏 display:none
     · bottom → grid 改单列 / flex 改 wrap，侧栏 flex: 1 1 100%
```

**关键设计：按 `left` 判断而不是按宽度。** 正文被挤窄的时候它反而**更窄**——如果按"宽的那一列是正文"来判，正好判反。位置是稳定的，宽度不是。

**为什么设 `done < 6` 的上限。** 全量遍历 `body *` 在 6000+ 元素的页面上代价不小，而真实页面里两栏容器通常不超过 2 个。找到 6 个就收手，把开销压在可接受范围内。

**配置项 `sideColumn`：**

| 值 | 行为 | 适合 |
|---|---|---|
| `'bottom'`（默认）| 侧栏移到正文下方 | 想保留"关于作者""相关推荐" |
| `'hide'` | 侧栏直接隐藏 | 只想纯读正文 |
| `'keep'` | 不动 | 排查问题 / 觉得误判时关掉 |

默认选 `bottom` 而不是 `hide`：侧栏里的"关于作者""相关阅读"在手机上并不无用，只是不该抢占正文的位置。挪到下面既解决了挤压，也不丢信息。真想要纯净阅读，一行配置就能切到 `hide`。

---

## 6. 测试体系

### 6.1 模拟环境怎么搭

没有真机，用 Playwright + Chromium 无头浏览器模拟 Pixel 7。关键是**精确复现 Kiwi 桌面模式的视口行为**：

```python
# Kiwi「桌面版网站」：屏幕 393 宽，Chrome 强制 layout viewport = 980，整页缩到 0.4
DESKTOP_MODE = dict(viewport={"width":980,"height":2130},   # layout viewport 被撑到 980
                    screen={"width":393,"height":852},      # 但物理屏幕只有 393
                    device_scale_factor=2.625,
                    has_touch=True,
                    user_agent=UA_DESKTOP)                  # 桌面 UA，拿桌面版 HTML

# 普通移动模式（对照组）
MOBILE_MODE = dict(viewport={"width":393,"height":852},
                   screen={"width":393,"height":852},
                   device_scale_factor=2.625,
                   is_mobile=True, has_touch=True,
                   user_agent=UA_MOBILE)
```

注意 `height: 2130` 的由来：屏幕高 852 物理 px，缩放比 0.4，所以 layout viewport 高 = 852 / 0.4 = 2130。

### 6.2 测量指标

定义了 6 个量化指标，每个都直接从 `getComputedStyle` 和真实布局读取，不依赖脚本自报：

| 指标 | 计算方式 | 健康值 |
|---|---|---|
| `zoom` | `getComputedStyle(html).zoom` | 桌面模式 ≈ 2.49 |
| `cssW` | `clientWidth / zoom` | 393 |
| `overflowX` | `scrollingElement.scrollWidth - clientWidth` | 0 |
| `overflowCount` | 遍历 `body *`，`offsetWidth > cssW + 2` 的元素数 | 0 |
| `textLen` | `body.innerText.length` | 与基线接近（内容别被删没了） |
| `pageerror` | 页面 JS 错误 | 无脚本自身引入的错误 |

**为什么用 `offsetWidth` 而不是 `getBoundingClientRect()`**：后者在 zoom 下返回放大 2.49 倍的值，会把正常元素误判成超宽。这正是 2.3 节说的陷阱二。

固定元素（`position: fixed`）单独处理：`getBoundingClientRect().width / zoom` 折算回基准坐标系。

### 6.3 测试用例矩阵

| 维度 | 取值 |
|---|---|
| 浏览器模式 | 桌面模式（勾桌面版）/ 移动模式（不勾） |
| 脚本配置 | 无脚本（基线）/ v3.1 / v0.1 |
| 页面 | `question/19550225`（问题页）/ `explore`（发现页） |
| 生命周期 | 首屏 / 滚动懒加载后 / 旋屏 / 转回竖屏 / 站内跳转后 |

共 12 个主用例 + 4 个旋屏/导航用例。

### 6.4 测试脚本清单

| 文件 | 用途 |
|---|---|
| `lib.py` | 公共库：两种模式的上下文配置、测量 JS、UA 常量 |
| `forensics.py` | 现场取证：选择器存活率、硬编码 min-width、vw/vh 规则数 |
| `final_regress.py` | 主回归：3 配置 × 2 页面 × 2 模式，输出量化对比 |
| `test_selfcheck.py` | 旋屏 / 站内跳转后的稳定性，验证 `__zhihuFit()` 自检入口 |
| `diag_spa.py` | 诊断 `screen.width` 读数污染问题 |
| `test_mobile.py` | 移动模式专项对照 |
| `shot3.py` | 生成三联对比截图 |

每个测量点都包含：滚到 800/1800/3000 触发懒加载 → 滚回顶部 → 等 700ms 稳定 → 测量。

---

## 7. 测试结果

### 7.1 桌面模式：三方对比

这是最核心的一组数据。Kiwi 勾选「桌面版网站」，模拟 Pixel 7：

| 配置 | 页面 | zoom | 布局宽 | 文档滚动宽 | 横向溢出 | 超宽元素 | 文本量 |
|---|---|---:|---:|---:|---:|---:|---:|
| 无脚本 | 问题页 | 1 | 980 | 1032 | **52** | **13** | 6818 |
| v3.1 | 问题页 | 1 | 980 | 1032 | **52** | **13** | 6798 |
| **v0.1** | 问题页 | **2.4936** | **393** | 980 | **0** | **0** | 6230 |
| 无脚本 | 发现页 | 1 | 980 | 1032 | **52** | **35** | 4517 |
| v3.1 | 发现页 | 1 | 980 | 1032 | **52** | **35** | 4533 |
| **v0.1** | 发现页 | **2.4936** | **393** | 980 | **0** | **0** | 4248 |

几点解读：

- **v3.1 与基线逐项完全一致**，从第 4 节的根因分析看这是必然结果。
- v0.1 的 zoom 精确落在 2.4936（980/393 = 2.49361...），布局宽 393，横向溢出和超宽元素都归零。
- 文本量从 6818 降到 6230（-8.6%），这是隐藏侧栏、推广位、App 下载条造成的，属于预期内的内容裁剪，不是"页面被压坏了"。
- `pageerror` 里有 `x[H(...)][H(...)] is not a function`——这条在**无脚本基线下同样存在**，是知乎自己的脚本错误，与适配无关。v0.1 没有引入任何新的页面错误。

### 7.2 移动模式：脚本会不会帮倒忙

不勾选「桌面版网站」时，脚本应该"安静"。实测：

| 配置 | 页面 | zoom | 布局宽 | 横向溢出 | 超宽元素 | 文本量 |
|---|---|---:|---:|---:|---:|---:|
| 无脚本 | 问题页 | 1 | 393 | 0 | 0 | 6081 |
| v0.1 | 问题页 | 1 | 393 | 0 | 0 | 6083 |
| 无脚本 | 发现页 | 1 | 393 | **639** | **206** | 4046 |
| v3.1 | 发现页 | 1 | 393 | **639** | **206** | 4047 |
| **v0.1** | 发现页 | 1 | 393 | **0** | **0** | 3841 |

**意外收获**：知乎自己的移动端发现页在 393px 下横向溢出 **639px、206 个元素超宽**（`.ExploreHomePage` 硬编码 1000px、`.ExploreHeader` 1046px）。也就是说知乎的移动版页面在手机上本来就是破的——v3.1 对这个同样无能为力，v0.1 顺手修好了。

问题页上两者完全一致（0 / 0 / 文本量 6081 vs 6083），说明脚本在普通移动模式下**不做多余干预**。

### 7.3 旋屏与站内跳转

这组测试暴露了首个内部版本（代号 v4.0）的两个真 bug，修复过程见第 8 节。修复后的 v0.1 表现：

| 状态 | zoom | 基准宽 | 布局宽 | 横向溢出 | 超宽元素 |
|---|---:|---:|---:|---:|---:|
| 初始竖屏 | 2.494 | 393 | 980 | 0 | 0 |
| 旋到横屏 | 2.173 | 980 | 2130 | 0 | 0 |
| 转回竖屏 | 2.494 | 393 | 980 | 0 | 0 |
| 站内跳转后 | 2.494 | 393 | 980 | 0 | 0 |
| 跳转后 +3 秒 | 2.494 | 393 | 980 | 0 | 0 |

四种状态下 zoom 稳定在 2.49，溢出恒为 0。

> 横屏行 zoom=2.173、基准宽 980 是测试环境的特性：Playwright 在 `set_viewport_size` 之后会把 `screen` 一并改成 2130×980，所以"屏幕"被模拟成了 980 宽。真机横屏时 `screen` 为 852×393，脚本会算出 zoom = 980/852 ≈ 1.15、基准宽 852。

### 7.4 两栏布局（专栏页）

专栏页在测试环境里被登录墙挡住（详见第 9 节），改用结构与类名一致的**复刻测试页**验证。测试页含两个变体：flex（侧栏 `flex: 0 0 296px` 不收缩）和 grid（列宽硬分配 `1fr 296px`），类名刻意用 emotion 风格的哈希名（`.css-wrap01`），不含任何语义 class。

**flex 变体：**

| 状态 | 主列宽 | 侧栏宽 | 侧栏位置 |
|---|---:|---:|---|
| 无脚本 | 636 | 312 | 右侧 ✗ |
| 装脚本 · 移到底部 | 361 | 361 | top=782（正文下方）✓ |
| 装脚本 · 隐藏 | 361 | — | 已隐藏 ✓ |

**grid 变体：**

| 状态 | `grid-template-columns` | 主列宽 | 侧栏位置 |
|---|---|---:|---|
| 无脚本 | `636px 296px` | 636 | 右侧 ✗ |
| 装脚本 · 移到底部 | `361.006px` | 361 | top=578（正文下方）✓ |
| 装脚本 · 隐藏 | `361.006px` | 361 | 已隐藏 ✓ |

两种变体在两种模式下都达标，主列统一撑满 361px（393 − 左右各 16px padding）。

### 7.5 视觉验证（子代理读图）

量化指标之外，另派了支持读图的子代理（Hy3 模型）复核截图。这轮复核不是走过场——**它揪出了第一轮取证的真实漏洞**：

| 轮次 | 子代理结论 |
|---|---|
| 第一轮 | flex 变体确认生效；但**截图没拍到 grid 变体**（落在 393px 视口之外），"grid 修复生效"当时**没有视觉证据** |
| 第二轮 | grid 变体单独成页重截，确认修复生效：正文撑满整行，侧栏垂直堆叠在下方（bottom 模式）/ 完全消失（hide 模式），无重叠、无裁切、无横向滚动；与 flex 变体效果一致 |

教训记在第 9 节：**量化指标说"通过了"，不等于视觉证据说"看见了"**。

### 7.6 自检入口

`window.__zhihuFit()` 返回当前状态的快照，用于真机自查：

```js
{
  屏幕宽: 393,      // 物理屏幕宽（CSS px）
  layout视口: 980,  // Chromium 撑开后的 layout viewport
  zoom: 2.494,      // 实际应用的 zoom
  基准宽: 393,      // 布局基准宽度
  横向溢出: 0,      // ← 关键指标，应为 0
  文档宽: 980,
  基准高: 854       // 基准坐标系下的可视高度
}
```

---

## 8. 过程中发现并修复的缺陷

按发现顺序排列。前四个是 v3 遗留的（继承问题），后三个是 v4 自己开发过程中测出来的。

| # | 缺陷 | 发现方式 | 影响 | 修复 |
|---|---|---|---|---|
| 1 | v3 `document.head` 为 null 导致整体崩溃 | 量化对比 + pageerror | **脚本 100% 失效** | `injectStyle()` 三重回退 |
| 2 | v3 选择器 53% 失效 | 现场取证统计命中数 | 大量规则空转 | 改用结构选择器 + JS 按宽度收拾 |
| 3 | v3 用 `getBoundingClientRect()` 测宽 | 代码审查 | zoom 下会误判并压塌页面 | 改用 `offsetWidth` |
| 4 | v3 把 `svg` 设成块级居中 | 代码审查 | 顶栏图标散架 | 媒体规则限定内容容器，排除 svg |
| 5 | `maxScan: 4500` 截断扫描 | 首轮测试 `count=203` | 回答列表压根没扫到 | 去掉截断 + 读写分离多轮收敛 |
| 6 | `fixHeader is not defined`（拼写错误） | pageerror | `applyAll()` 抛错 → MutationObserver、resize 监听**全都没注册** | 改为 `fitHeader` + 全链路 `safe()` 包裹 |
| 7 | 旋屏后适配永久失效 | 旋屏自检 | 旋一次屏 zoom 就变 1 且回不来 | 设备短边锚定 + 物理不可能性兜底 |
| 8 | `vh/vw` 折算只能做一次 | 旋屏自检的连带分析 | 旋屏后单位无法还原 | 改用 CSS 变量 `calc(N * var(--zf-vh))` |
| 9 | **grid 列宽硬分配，侧栏隐藏后列还在** | 专栏页复刻测试 | 侧栏 `display:none` 后主列只剩 **49px**，`hide` 模式比不改更糟 | 容器兜底 `grid-template-columns: 1fr` |
| 10 | 专栏页右侧栏挤占正文 | 用户反馈 | 正文被挤成窄条，侧栏占大半屏 | 新增 `fitColumns()` 结构化识别两栏 |
| 11 | **脚本自己把正文压塌（694px → 24px）** | 用户真机诊断数据 | 正文变成一条竖线，**比不装脚本还糟** | 新增 `fixFlexRows()`：容器换行，内容块各占一行 |
| 12 | 前两轮修复都建立在错误假设上 | 用户真机诊断数据 | 连做两版无效修复 | 见第 13 章 |

### 8.1 缺陷 5 详解：203 个超宽元素的假象

首轮 v4 测试出现一个矛盾结果：`overflowX=0` 但 `overflowCount=203`。

一开始以为是检测逻辑有问题。排查后发现两件事：

1. **`overflow-x: clip` 制造了假象**。它把溢出内容裁掉了，所以 `scrollWidth` 不再超出 `clientWidth`，`overflowX` 显示 0——但元素**实际宽度还是 1032px**，内容只是被裁掉看不见了。这反而说明指标只看 `overflowX` 是不够的，必须同时看 `overflowCount`。
2. **扫描被 `maxScan: 4500` 截断了**。滚动加载后页面有 6000+ 元素，回答列表那批压根没扫到。

修掉截断、改成读写分离多轮收敛后，203 → 0。

顺便：如果当时只看 `overflowX=0` 就宣布成功，交付的就是个"内容被裁掉"的残次品。**单一指标会骗人**。

### 8.2 缺陷 6 详解：一个字母引发的连锁故障

`applyAll()` 里调用了 `fixHeader()`，但函数实际名是 `fitHeader()`。这个拼写错误的后果远超预期：

```
applyAll() 抛 ReferenceError
  → boot() 中断
    → MutationObserver 没注册（懒加载内容永远不会被适配）
    → resize / orientationchange 监听没注册（旋屏后永远不重算）
    → 定时重扫没注册
    → 脚本只在空骨架时跑了一次就彻底死掉
```

因为首屏骨架简单，单次运行看起来"好像有效果"，很容易蒙混过关。是 pageerror 监听把它揪出来的。

修复除了改对名字，更重要的是给所有阶段加 `safe()` 包装——**单点失败不应该拖垮全局**。

### 8.3 缺陷 7 详解：旋屏后 zoom 永远回不来

首个内部版本（代号 v4.0）完成后做旋屏自检，结果：

```
初始:   zoom=2.494         ← 正常
旋屏后: zoom=1             ← 失效
转回竖屏: zoom=1           ← 再也回不来
```

"再也回不来"是致命的——用户旋一次屏，整个脚本就废了，必须手动刷新。

诊断输出揭示了原因：

```
[初始竖屏] screen=393x852   inner=980x2130   layout=980
[旋屏横]   screen=2130x980  inner=2130x980   layout=2130   ← screen 被改了
[转回竖]   screen=980x2130  inner=980x2130   layout=980    ← screen 没恢复
```

部分内核（含 Kiwi）在旋屏时会让 `screen.width` 返回 **layout viewport 的宽度**而不是物理屏幕宽。采到这个脏值后：

```
sw = 980, vw = 980  →  ratio = 1  →  needZoom = false  →  zoom = 1
```

而且只要 `needZoom` 变 false，CSS 就走"不缩放"分支，zoom 再也回不来。

修复分三层：

**第一层：以设备短边为锚。** 手机的物理短边（393）在竖屏横屏下恒定不变，是最可靠的锚点。只有读数落在合理手机区间 `[280, 700]` 且不超过 layout viewport 时才刷新缓存：

```js
if (lo >= 280 && lo <= 700 && lo <= vw) S.SHORT = lo;
```

脏值 980 不在区间内 → 缓存保持 393。

**第二层：方向用 viewport 判定。** `matchMedia('(orientation: landscape)')` 基于 viewport，桌面模式下 viewport 会跟着旋，可靠。横屏时取长边，但要求长边是短边的 1~3 倍且小于 layout viewport，否则退回短边。

**第三层：物理不可能性兜底。** 手机屏幕宽**不可能** ≥ layout viewport（桌面模式恒 980 > 393）。一旦读到，无条件退回缓存短边：

```js
if (sw >= vw) {
  var fb = S.SHORT || Math.round(vw / 2.494);   // 连缓存都没有就按 Chrome 默认比例反推
  sw = Math.max(280, Math.min(fb, vw));
}
```

**第四层：桌面模式判定粘性锁定。** 一旦判定为桌面模式就不再翻转，避免单次脏读数把状态打回去：

```js
if (ratio > 1.25) S.desktop = true;
var needZoom = S.desktop === true;
```

这里有个调试插曲：第一版兜底我加了 `S.desktop === true` 的前置条件，结果漏掉了"首屏就遇到脏值"的情况（此时 `S.desktop` 还没被设过）。诊断脚本打印出 `screen=980x2130 / layout=980` 才发现问题，去掉前置条件后通过。

### 8.4 缺陷 9 详解：grid 的列宽是硬分配的

这个缺陷是做专栏页适配时挖出来的，反直觉程度不亚于缺陷 5。

**背景。** 专栏页是两栏结构：左边正文，右边"关于作者""大家都在搜"。在 393px 的手机上，侧栏吃掉 296px，正文只剩一条缝。修复思路很直接——把侧栏挪走。

**第一步看着是成功的。** flex 布局下，把侧栏 `display:none` 之后主列立刻撑满 361px。按同样的逻辑去处理 grid 变体，量化数字却给出了完全相反的结果：

```
grid 变体，侧栏 display:none 之后：
  主列 offsetWidth = 49px     ← 比不处理还窄
```

**根因。** grid 的列宽是**在容器上硬分配**的，跟子元素在不在没有关系：

```css
.wrap { display: grid; grid-template-columns: 1fr 296px; }
```

`1fr` 和 `296px` 已经把容器劈成了两半。`display:none` 只是让侧栏**不渲染**，那一列 296px 的**槽位还留着**。剩下给 `1fr` 的就只有 `393 - 296 - gap - padding` ≈ 49px。

也就是说：**在 grid 里隐藏侧栏，反而会把正文压得比原来更窄。** 如果不单独测 grid 变体，这个"修复"会作为一桩善事被交付出去。

**修复。** 处理侧栏时必须同时改容器：

```js
var bcs = getComputedStyle(box);
if (bcs.display === 'grid' || bcs.display === 'inline-grid') {
  box.style.setProperty('grid-template-columns', '1fr', 'important');
} else {
  box.style.setProperty('display', 'flex', 'important');
  box.style.setProperty('flex-wrap', 'wrap', 'important');
}
```

除了 JS 兜底，CSS 层也加了基于 `:has()` 的容器规则（Chrome 105+，Kiwi 内核满足）：

```css
div:has(> aside), div:has(> [class*="SideColumn"]), ... {
  grid-template-columns: 1fr !important;
  flex-wrap: wrap !important;
}
```

**验证。** 用 `getComputedStyle(wrap).gridTemplateColumns` 直接读容器的真实列宽，而不是只看主列的 `offsetWidth`：

| 状态 | `grid-template-columns` | 主列 | 侧栏 |
|---|---|---|---|
| 无脚本 | `636px 296px` | 636 | 在右侧 |
| 装脚本 · 移到底部 | `361.006px` | 361 | top=578（正文下方）|
| 装脚本 · 隐藏 | `361.006px` | 361 | 已隐藏 |

列宽从"两列"变成"单列"，这是修复生效的**直接证据**；主列宽度只是**间接证据**，容易被折叠、换行等其他因素干扰。

---

## 9. 工程踩坑记录（与脚本无关）

这些是开发过程中在工具链上踩的坑，记录备查。

| 坑 | 现象 | 解法 |
|---|---|---|
| Shell heredoc 吞掉 `${}` | 用 `cat > x.py <<'EOF'` 内联 JS 时报 `Bad substitution` | 改用文件写入方式落盘，JS 用 `r"""..."""` 原始字符串 |
| PIL `crop` 坐标越界 | `Coordinate 'lower' is less than 'upper'`，对 853 高的图裁 1000~3500 | 改用 `full_page=True` 截图再分段裁 |
| `DecompressionBombWarning` | 发现页 full_page 达 2699×41181（111M 像素）超 PIL 限制 | 仅为警告；缩放到 393 宽分段规避 |
| 知乎反爬 | `curl` 直连 403，`-L` 跳 `signin` | 用真实浏览器访问；403 但 SSR 内容完整时仍可用于布局测试 |
| **主模型不支持读图** | 多次读取 PNG 均被系统拦截 | 改派支持读图的子代理（Hy3 模型）做视觉校验 |
| 专栏页被登录墙 + IP 风控双重拦截 | 换 UA / 大屏 / 带 Referer 均返回"您当前请求存在异常" | 构造结构与类名一致的复刻测试页验证逻辑 |
| **截图拍不到目标变体** | 两个变体上下排列，grid 变体落在 393px 视口可见范围之外 | 单独成页再截图，保证目标进入视口 |

最后两条值得展开。

**关于读图。** 主模型看不了图，所以一开始所有"视觉效果"的判断都建立在量化指标上——zoom 值、逐元素 `offsetWidth`、溢出计数、文本长度、容器的 `grid-template-columns`。这些能证明"布局正确"，但证明不了"看起来舒服"。后来改派支持读图的子代理复核，果然揪出了第一个版本的漏洞：截图里根本没拍到 grid 变体，也就是说**我引以为据的那条核心修复，当时其实没有任何视觉证据**。量化指标说"通过了"，视觉证据说"没看见"——两者缺一不可。

**关于复刻测试页。** 专栏页在测试环境里被登录墙挡住，拿不到真实内容。与其在风控上硬耗，不如按知乎的 DOM 结构复刻一个测试页：两个变体（flex / grid），用 emotion 风格的哈希类名（`.css-wrap01`），不依赖任何语义 class。这样既能验证逻辑，又顺带证明了"启发式识别不依赖类名"这个设计目标是成立的。

代价要说清楚：**复刻页验证的是逻辑，不是真实页面的观感**。真机上的专栏页是否如预期，仍需你亲自看一眼。

---

## 10. 已知限制与未覆盖场景

诚实列出边界，避免"看起来什么都能行"：

| 限制 | 说明 |
|---|---|
| **未做真机验证** | 全部测试在 Playwright + Chromium 131 模拟环境完成。Kiwi 的具体内核版本、桌面模式实现细节可能与之有差异 |
| **未做肉眼观感验证** | 主模型不支持读图，改由支持读图的子代理复核。字号是否舒服、顶栏裁剪是否合理、留白是否协调，仍建议你实际看一眼 |
| **专栏页崩塌为复刻页验证** | 真实专栏页被登录墙 + IP 风控拦截。0.3.0 的崩塌修复是在按真机 DOM 层级构造的复刻页上复现并验证的（正文 24px → 345px），**真机效果待用户确认** |
| **只测了两个真实页面** | 问题页、发现页。个人主页、搜索结果页、评论区展开态未覆盖 |
| **评论区未专门测试** | 评论区是知乎结构最复杂的部分之一，且多为懒加载弹层，可能需要补规则 |
| **模拟环境的 `screen` 污染** | 测试环境在 `set_viewport_size` 后会污染 `screen` 读数。这个问题在真机上不会发生，但意味着横屏路径的真机行为未经完全验证 |
| **知乎改版风险** | 虽然主力逻辑基于"实测宽度"而非选择器，但 `fitHeader()` 依赖 `header.AppHeader nav` 结构，`hideSidebar` 依赖若干语义 class，这些仍可能随改版失效 |
| **`table` 用 `display:block`** | 表格被改成块级 + 横向滚动，列宽可能不够理想，但保证了不会撑破页面 |

如果出现问题，在控制台执行 `window.__zhihuFit()`，把返回值（特别是 `横向溢出` 不为 0 时）连同页面 URL 发我，可以针对性补规则。

---

## 11. 附录：文件清单与复现步骤

### 11.1 交付文件

```
/workspace/zhihu-mobile/
├── zhihu-desk2mob.user.js      # 主脚本 v0.3.0
├── zhihu-diag.user.js          # 诊断工具（临时装，真机取证用）
├── README.md                    # 仓库首页
├── 安装说明.md                  # 安装与使用说明
├── 制作与测试记录.md            # 本文
├── 测试脚本/
│   ├── lib.py                  # 公共库：模式配置 + 测量 JS
│   ├── forensics.py            # 现场取证
│   ├── final_regress.py        # 主回归（12 用例）
│   ├── test_selfcheck.py       # 旋屏 / 导航稳定性
│   ├── diag_spa.py             # screen 读数诊断
│   ├── diag_zhuanlan.py        # 专栏页布局诊断
│   ├── test_mobile.py          # 移动模式对照
│   ├── test_one.py             # 单脚本快速验证
│   ├── shot3.py                # 三联对比截图生成
│   ├── baseline.json           # 无脚本基线数据
│   ├── regress.json            # 完整回归数据
│   └── forensics.json          # 取证数据
└── 测试截图/
    ├── desktop-question.png    # 问题页：无脚本 / v3 / v4 三联
    ├── desktop-explore.png     # 发现页：无脚本 / v3 / v4 三联
    ├── mobile-explore.png      # 移动模式：无脚本 / v4
    ├── 两栏布局-无脚本.png          # flex + grid 两变体，对照组
    ├── 两栏布局-侧栏移到底部.png     # sideColumn = 'bottom'
    ├── 两栏布局-侧栏隐藏.png         # sideColumn = 'hide'
    ├── grid变体-无脚本.png           # grid 变体单独成页，对照组
    ├── grid变体-侧栏移到底部.png     # grid：侧栏移到底部
    └── grid变体-侧栏隐藏.png         # grid：侧栏隐藏
```

### 11.2 复现步骤

```bash
# 环境
pip install playwright && playwright install chromium

# 1. 现场取证（选择器存活率、硬编码宽度）
python3.11 测试脚本/forensics.py

# 2. 主回归：3 配置 × 2 页面 × 2 模式，约 90 秒
python3.11 测试脚本/final_regress.py

# 3. 旋屏 / 站内跳转稳定性
python3.11 测试脚本/test_selfcheck.py

# 4. 生成对比截图
python3.11 测试脚本/shot3.py
```

### 11.3 版本演进

| 版本 | 性质 | 主要变化 |
|---|---|---|
| v3.1 | 参考脚本（非本仓库代码） | 用户提供的参考。实测完全不生效（head 为 null 导致 IIFE 崩溃） |
| v4.0.0 | 内部开发代号 | 重写。修好注入、改用结构选择器 + JS 宽度扫描、读写分离多轮收敛、错误隔离 |
| v4.1.0 | 内部开发代号 | 修「旋屏后适配永久失效」（设备短边锚定 + 三重兜底）；修「vh/vw 折算只能做一次」（改 CSS 变量） |
| **0.1.0** | **对外首个发布版** | 内容等同内部 v4.1.0。本仓库的版本号从这里开始计 |
| **0.2.0** | 功能增强 | 新增 `fitColumns()` 两栏布局识别（不依赖类名）；新增 `sideColumn` 配置（bottom / hide / keep）；修「grid 列宽硬分配，侧栏隐藏后主列仅剩 49px」；侧栏 CSS 拆分为"始终隐藏的推广件"与"按模式处理的侧栏"两组，并加 `:has()` 容器兜底 |
| **0.2.1** | 可观测性 | 角标加版本号；新增 `zhihu-diag.user.js` 诊断工具（真机取证用，无需账号） |
| **0.3.0** | **重要修复** | 修「脚本自己把正文压塌」（694px → 24px）：新增 `fixFlexRows()`，检测到"内容多却被压得窄"的 flex item 时让容器换行。同时修正 0.2.0 建立在错误假设上的两栏处理 |

---

## 12. 真机反馈的处理方式

0.2.0 交付后用户反馈"专栏页右边栏位置没变"。这一节记录为什么不直接要账号，以及替代方案。

### 12.1 沙箱拿不到真实专栏页

先排除了"我自己复现"这条路。三个 URL 全部试过：

```
[1] https://zhuanlan.zhihu.com/p/2044268985798104354  → bodyTxt=456，侧栏候选 0 个
[2] https://zhuanlan.zhihu.com/p/662218709            → bodyTxt=456，侧栏候选 0 个
[3] https://www.zhihu.com/p/2044268985798104354       → 404
```

即使用真实浏览器（非 curl）、先访问首页建立会话 cookie 再跳转、桌面 UA 全套指纹，专栏页仍被重定向成一个 456 字符的空壳。**登录墙 + IP 风控，沙箱环境绕不过去。**

### 12.2 为什么不建议给账号

用户主动提出"要不要我登录知乎，手机号验证码给你"。评估后否决，理由：

1. **账号会留在我的环境里**——cookie 存在沙箱，会话期内等于我随时能以你的身份操作。
2. **异地登录会触发风控**，可能给账号带来麻烦。
3. **收益不成比例**——我需要的是**DOM 结构**，不是账号权限。结构信息完全可以在不登录的前提下拿到。

关键判断：**要的是数据，不是身份**。既然只要数据，就应该设计一条只取数据、不碰身份的路径。

### 12.3 替代方案：诊断脚本

于是做了 `zhihu-diag.user.js`——不修改页面，只读结构 + 复刻 `fitColumns()` 的判定过程，把每一步摊开：

| 输出 | 作用 |
|---|---|
| 版本号 / zoom / 布局宽 / 横向溢出 | 先确认主脚本到底有没有在工作（很多"没生效"是装的旧版）|
| 侧栏候选（按类名）| 页面里有没有 `aside`、`SideColumn`，各多宽多高 |
| **扫描统计** | 七个拒绝分支各拦下多少容器 |
| **最接近命中的候选** | 卡在哪一关 + 主列/侧栏的具体宽高字数 |
| **够宽容器的直接子元素** | 两栏到底藏在第几层 |

最后一条是核心。**`fitColumns()` 有个隐含假设：主列和侧栏是同一个父容器的直接子元素。** 真实知乎的 DOM 嵌套往往比复刻测试页深得多，如果中间隔了一层，这个假设就不成立，算法会静默跳过——这正是"复刻测试页全绿、真机没反应"最可能的原因。

诊断脚本还暴露了一个原本看不见的问题：**`fitColumns()` 有 7 个 `continue` 分支，全是静默跳过**。出了事根本无从下手。把拒绝原因计数化，是这个工具最有价值的副产品。

### 12.4 顺带补的缺口

排查时发现一个基础缺陷：**角标不显示版本号**。用户说"没生效"时，我连他装的是 0.1.0 还是 0.2.0 都无法确认——这种低级的可观测性缺口，让所有后续排查都建立在猜测上。改成 `✓ v0.3.0 393px ×2.49`。

---

## 13. 真正的元凶：脚本自己把正文压塌了

第 12 章的诊断工具上线后，用户跑了两轮，第一轮就推翻了我的假设。

### 13.1 假设是怎么被推翻的

用户原话是"右侧栏的『关于作者』『大家都在搜』占据了大部分屏幕"。我据此认定专栏页是两栏布局，做了 `fitColumns()`，在复刻测试页上跑得很好。

真机数据出来后：

```
── 侧栏候选（按类名命中 0 个）──
    （无。说明页面里没有带 SideColumn/SideBar/aside 类名的元素）

第7层 父 <div> .css-yq5nsh   w=55   kids=2
   ┊ <article> .Post-Main    w=24   txt=5633   ← 正文
   ▶ <div>                   w=15   txt=1889   ← 评论区
```

**一个侧栏都没有。** `.css-yq5nsh` 只有两个子元素，上下排列。

真正的问题是：**正文有 5633 个字符，宽度只有 24px**。全局扫描 1327 个元素，1019 个宽度不足 236px。

**用户描述的是现象，我把它翻译成了"两栏布局"——这个翻译是错的。**

### 13.2 定位：宽度是在哪一层塌的

第二轮诊断（v3，改查宽度链）：

```
└─ <main> .App-main          实测宽=393
  └─ <div> .Post-content     实测宽=393
    └─ <div> .css-kjzwqj     实测宽=393  disp=flex
      └─ <div> .css-c0fani   实测宽=55   ◀◀ 断点！宽度从 393 掉到 55
        └─ <article> .Post-Main  实测宽=24
```

父是 **flex 容器**，子从 393 直接掉到 55。393 ÷ 7 ≈ 56——**是 393px 被多个 flex item 分摊了**。

### 13.3 本地复现

按真实类名和层级构造 `testpage_flexrow.html`（flex 容器 + 7 个带硬编码宽度的 item），装脚本跑：

| 子元素 | 无脚本 | 装脚本后 | min-width |
|---|---:|---:|---|
| `.css-c0fani`（正文） | 710 | **16** | **0px** ← 被脚本清零 |
| `.css-other1` | 316 | 316 | 300px（没动） |
| `.css-other2` | 316 | 316 | 300px（没动） |
| `.css-other3~6` | 276~216 | 276~216 | 260~200px（没动） |

`article.Post-Main` 694 → **24**，`.RichText` 678 → **0**。

### 13.4 根因

`fixWidths()` 的处理条件是 `widths[i] > base + 1`，也就是**只处理超宽的那一个**。它给 `.css-c0fani` 做了两件事：

```js
if (parseFloat(cs.minWidth) > base) el.style.setProperty('min-width', '0', 'important');
if (cs.flexShrink === '0')          el.style.setProperty('flex-shrink', '1', 'important');
```

其余兄弟宽度不足 394，**一个都没被处理**，min-width 原样保留。

于是 flex 收缩时：

```
总需求宽度 = 2326，容器 = 361，需要收缩 1965px
others 有 min-width 保护（合计 1520），最多只能让出 176px
剩下的 1789px 收缩压力，全部落在唯一被解锁的 .css-c0fani 身上
→ 被压到 16px
```

**`min-width: 0` 本意是打破硬编码宽度，副作用是让这个 item 成为唯一的"可压缩对象"，替所有兄弟承担收缩。**

讽刺的是：如果不处理 min-width，页面会横向溢出；处理了，正文就被压塌。两个都不可接受。

### 13.5 修复：`fixFlexRows()`

判据——容器里存在**"内容很多但被压得很窄"**的 item：

```js
if ((kid.innerText || '').trim().length < 200) continue;   // 不是内容块
if (kid.offsetWidth >= boxW * 0.3) continue;               // 没被压塌
```

两条一起，只会在**被压塌的内容块**上命中。顶栏图标、导航条文本量不够，不会被误伤。

处理：容器改成换行，内容块各占一整行。

```js
box.style.setProperty('flex-wrap', 'wrap', 'important');
el.style.setProperty('flex', '1 1 100%', 'important');
```

**手机上本就该纵向堆叠，横向硬挤 7 列没有任何意义。** 这个修复顺带把"侧栏移到正文下方"也做了——比 `fitColumns()` 的启发式更直接，因为它看的是真实的空间分配结果，而不是猜结构。

执行顺序必须放在 `fixWidths()` **之后**——崩塌是它造成的，得先发生才能检测到。

### 13.6 验证

| | 正文宽 | `.RichText` | 容器 wrap | 横向溢出 |
|---|---:|---:|---|---:|
| 无脚本 | 694 | 678 | nowrap | 1362 |
| 修复前 | **24** | **0** | nowrap | 0 |
| 修复后 | **345** | **321** | **wrap** | 0 |

回归：问题页 / 发现页桌面模式仍为 `zoom=2.4936`、溢出 0、超宽 0；移动模式无退化。两栏测试页、嵌套测试页均不受影响。

子代理视觉复核：修复前"文章标题和正文完全不可见，已被压缩成极窄的竖条"；修复后"正文恢复全宽，每行文字正常折行，段落清晰可读，变为手机端单列阅读布局"，未发现新问题。

### 13.7 教训

**用户报告的是现象，不是诊断。** 我把"右侧栏占据大部分屏幕"直接翻译成了"两栏布局"，然后在自己搭的、恰好符合这个假设的复刻测试页上验证通过——**这是典型的自我印证**。

两版修复（0.2.0 的 `fitColumns`、0.2.1 的诊断工具）都建立在这个没被验证过的假设上。直到让真机把 DOM 结构打出来，才发现页面上压根没有侧栏。

教训两条：

1. **先取证，再动手。** 现象和根因之间可能隔着一层误解，尤其是当你已经有了一个"看起来很合理"的解释时——那正是最该怀疑的时候。
2. **复刻测试页会骗人。** 它是按我的假设搭的，所以在它上面跑通只证明了"我的假设内部自洽"，没证明"假设是真的"。真正的问题是"脚本把正文压塌了"，而这个在我的复刻页里根本不会发生。

---

*文档最后更新：2026-09-02 · 对应脚本版本 v0.3.0*
