# 知乎桌面版 → 手机宽度适配（Kiwi / 油猴脚本）

脚本文件：`zhihu-desk2mob.user.js`（v0.2.0）

## 一、安装（Kiwi 浏览器）

Kiwi 支持直接安装 userscript，两种办法任选：

### 方法 A：本地文件安装（推荐，最快）
1. 把 `zhihu-desk2mob.user.js` 拷到手机存储（比如 `Download/`）。
2. 打开 Kiwi → 地址栏输入 `kiwi://extensions`，打开右上角「开发者模式」。
3. 把 js 文件**直接拖进**这个页面（或用 Kiwi 打开该文件的 `file://` 地址，会提示安装）。
4. 确认安装。

### 方法 B：先装 Tampermonkey
1. Kiwi 访问 Chrome 应用商店装 **Tampermonkey**。
2. Tampermonkey 面板 → Utilities → Import → 选这个 js 文件。

> 装 Kiwi 原生的 userscript 时，`@run-at document-start` 是支持的，脚本必须在这个时机跑才有效——这也是本次重写的关键点之一。

## 二、使用

1. Kiwi 打开 `www.zhihu.com`。
2. 菜单里勾选 **「桌面版网站」**（不勾选也能用，脚本会自动跳过缩放补偿，只做内容适配）。
3. 刷新一次页面即可。

右上角会有一个小角标 `✓ 393px ×2.49`，点一下可以**暂停 / 恢复**适配（对比原版时很方便）。
不想要角标：编辑脚本顶部 `CONFIG.debug = false`。

## 三、可调参数（脚本顶部 CONFIG）

| 参数 | 默认 | 说明 |
|---|---|---|
| `debug` | `true` | 右上角状态角标，点击可暂停/恢复 |
| `fitHeader` | `true` | 顶栏超宽时自动裁剪导航项 |
| `hideSidebar` | `true` | 隐藏悬浮按钮、App 下载条（右侧栏另见下一条）|
| `sideColumn` | `'hide'` | **右侧栏怎么处理**：`'hide'` 直接去掉（默认）/ `'bottom'` 移到正文下方 / `'keep'` 不动 |
| `hideRightRail` | `true` | 按**位置**兜底去掉右侧栏。现代知乎大量用 emotion 哈希类名，类名选择器盖不全，靠它补位 |
| `fixFlexRows` | `true` | 修「多列 flex 把正文压成一条」：容器换行，内容块各占一行 |
| `modalBackClose` | `true` | 弹层（「展开其他 N 条回复」等）：按手机返回键只关弹层，不退出页面 |
| `fixVUnits` | `true` | 修正 zoom 下失真的 `vw`/`vh` 单位 |
| `bodyFont` | `16` | 正文基准字号（px），觉得小就调大 |
| `sidePadding` | `12` | 页面左右留白 |
| `maxColumnDepth` | `8` | 两栏容器的最大 DOM 深度。页面骨架很浅、卡片内部很深，超过这个深度一律不当两栏处理 |

## 四、自检

**先看角标**：右上角显示 `✓ v0.4.0 393px ×2.49`，开头就是版本号。报问题时把这个角标内容一起发我——很多"没生效"其实是装的旧版。

在 Kiwi 地址栏（或连电脑远程调试的 console）里执行：

```js
window.__zhihuFit()
```

返回示例（健康状态）：

```js
{ 屏幕宽: 393, layout视口: 980, zoom: 2.4936, 基准宽: 393, 横向溢出: 0, 文档宽: 393, 基准高: 852 }
```

关键是 **`横向溢出: 0`**。如果不是 0，把返回值发给我，我针对性补规则。

## 四·五、某个页面不对劲时：用诊断脚本

**不用给我你的知乎账号**。装这个临时工具，它把页面的真实结构和匹配过程摊开给你看，复制粘贴发我就行。

文件：**[`zhihu-diag.user.js`](./zhihu-diag.user.js)**（v2）

步骤：

1. 装 `zhihu-diag.user.js`（和主脚本同样的装法，两个可以共存）
2. 打开**有问题的那个页面**，等 1~2 秒
3. 顶部弹出诊断面板，点 **「复制诊断信息」**
4. 粘贴发我

面板里会显示：

- **版本检查** —— 直接判定主脚本是不是新版，旧版会提示更新地址
- **正文定位** —— 找到真正的正文容器（按 DOM 深度定位，不是按文本量），给出完整路径
- **祖先链反查** —— 从正文往上爬 7 层，每层列出全部兄弟元素的宽/left/top/文本量。两栏藏在哪一层一眼可见
- **右侧邻居** —— 和正文同层、位置更靠右、有实际内容的元素，即真正的侧栏
- **zoom / 布局宽 / 横向溢出** —— 确认主脚本是不是在正常工作
- **侧栏候选** —— 页面里有没有 `aside`、`SideColumn` 这类元素，它们的宽高位置
- **扫描统计** —— 每一步拒绝了多少个容器（"右侧不够宽""主列文本不多于侧栏""主列内容太少"……）
- **最接近命中的候选** —— 卡在哪一关、主列和侧栏各是多少宽多少字
- **够宽容器的直接子元素** —— 两栏到底藏在第几层

最后一条最关键：脚本假设**主列和侧栏是同一个父容器的直接子元素**。真实页面如果中间还隔了一层，这个假设就不成立，诊断结果会直接暴露出来。

跑完删掉即可。它不修改页面，只读结构。

### `sideColumn` 三选一

专栏页、问题页这类两栏结构，右侧栏（"关于作者""大家都在搜""相关问题"）在手机上会挤占正文。三个选项：

| 值 | 效果 | 适合 |
|---|---|---|
| `'hide'`（**默认**）| 侧栏**完全去掉** | 只想干净地读正文 |
| `'bottom'` | 侧栏移到**正文下方**，宽度撑满 | 想保留作者信息、相关推荐 |
| `'keep'` | 不动侧栏 | 觉得脚本误判了，用它排查 |

改法：编辑脚本顶部 `CONFIG.sideColumn` 那一行，改完刷新页面。

识别逻辑**不依赖知乎的类名**（改版就会失效），而是两条腿走路：

1. **看结构**：同一行里位置靠左、文本量大的那列当正文，靠右的那列当侧栏。
2. **从正文反查**（`hideRightRail`）：先找到页面上文本量最大的内容块当正文，再从它出发向上爬几层，检查每一层的兄弟元素，四条判据同时成立就判定是侧栏——宽度 ≥ 100px、位置在正文右侧、垂直方向与正文重叠、文本量比正文少。

第 2 条是 0.4.0 补的。因为真机实测「侧栏候选（按类名）命中 **0 个**」——现代知乎大量用 emotion 生成的哈希类名，光靠类名和位置分组根本盖不全。有了它，首页、问题页、专栏页的右侧栏都能去掉。

> **0.3.0 补充说明**：如果你看到"正文被压成一条竖线"，那不是侧栏挤的，而是脚本的宽度修复把正文压塌了——由 `fixFlexRows` 处理，`sideColumn` 管不了。详见[制作与测试记录](./制作与测试记录.md)第 13 章。

## 五、变化记录

### 0.4.0

- **右侧栏全部去掉（首页 / 问题页 / 专栏页）**，`sideColumn` 默认值从 `'bottom'` 改为 `'hide'`。
- 新增 `hideRightRail`：从正文反查祖先链的兄弟元素，按**位置**判定侧栏。填补了「类名选择器命中 0 个」的空白。
- **修：发现页卡片底部的「回答281 赞同35 评论」被误删。** 元凶是 `fitColumns` 的判据太松，把卡片内部的「内容 + 标签行」当成了两栏布局。加了三道约束：容器深度 ≤ 8、侧栏宽度不超过容器 55%、侧栏右边缘要顶到容器 70% 以外。
- **新增：弹层按返回键关闭。** 评论里的「展开其他 N 条回复」弹出的浮层，以前按返回键会直接退出整个页面，现在只关弹层、页面不动（URL 都不变）。
  - 实现上先踩了个坑：最初是「popstate 时 pushState 把历史顶回去」，但 popstate 触发时 `location.href` **已经变成后退后的地址**了，从 A 页点进 B 页开弹层会直接被送回 A 页。改成**弹层一出现就先压一条缓冲历史**，返回时消耗掉它，URL 自始至终不变。
  - 没有弹层时返回键行为完全不变，不会被劫持。
  - 浮层还会自动补一个 `✕ 关闭` 按钮（有些层压根没有能点的关闭入口）。

### 0.3.0（重要修复）

- **修：脚本自己把正文压塌了。** 专栏页正文从 694px 被压到 **24px**，比不装脚本还糟。
  根因是 `fixWidths()` 只处理超宽的那个元素（把它的 `min-width` 清零、允许收缩），
  其余兄弟的最小宽度保护原样保留，于是 flex 收缩时**全部压力都压在正文一个人身上**。
- 新增 `fixFlexRows()`：检测到「内容很多但被压得很窄」的 flex item 时，让容器改成换行、
  内容块各占一整行。手机上本就该纵向堆叠，横向硬挤 7 列没有意义。实测正文 24px → **345px**。
- 这条修复**顺带解决了侧栏挤压**——它看的是真实的空间分配结果，比猜结构更直接。

### 0.2.1

- 角标加上版本号（现在显示 `✓ v0.2.1 393px ×2.27`），排查"装的是不是最新版"时一眼可见。
- 新增诊断脚本 [`zhihu-diag.user.js`](./zhihu-diag.user.js)：某个页面不对劲时装一下，把真实 DOM 结构和匹配判定过程 dump 出来，复制发我即可定位，**不需要提供知乎账号**。

### 0.2.0

- **新增：专栏页等两栏布局的处理**。右侧栏会挤占屏幕把正文压成窄条，现在默认把侧栏移到正文下方。识别靠结构启发式（位置 + 文本量），不依赖类名，改版不会失效。
- **新增：`sideColumn` 配置项**，`'bottom'`（默认，移到底部）/ `'hide'`（直接隐藏）/ `'keep'`（不动）。
- **修：grid 布局下隐藏侧栏反而让正文更窄**。grid 的列宽是硬分配的，`display:none` 只让侧栏不渲染，296px 的列槽位还留着，主列只剩 49px。现在会同时把容器改成单列。

### 0.1.0（首个发布版）

- **相对参考脚本做了重写**：原脚本在 `@run-at document-start` 阶段因 `document.head` 为 `null` 而抛错，整个脚本中断——实测数据与不装脚本完全一致，等于完全没生效。这里重写了注入逻辑（`head` / `documentElement` 双回退 + MutationObserver + 轮询兜底）。
- **修：旋屏后适配永久失效**。部分内核旋屏时会让 `screen.width` 返回 layout 宽度而不是物理屏幕宽，一旦采到脏值 zoom 就被算成 1 且再也回不来。改为以「设备短边」为锚 + 物理不可能性兜底。
- **修：`vh/vw` 折算只能做一次**。原来把 `100vh` 直接替换成固定 px，旋屏后无法还原。改为替换成 `calc(100 * var(--zf-vh))`，方向变化时只更新 CSS 变量。

## 六、它具体解决了什么

Kiwi 勾了「桌面版网站」后，Chromium 会把 layout viewport 强行撑到 **980 CSS px**，再把整页缩到约 **0.4 倍**塞进 393px 的屏幕，于是字小到看不清、还要左右拖。脚本做四件事：

1. **抵消缩放**：`html { width: 393px; zoom: 980/393 }` → 393 × 2.494 × 0.4 ≈ 393，净效果 1 CSS px = 1 设备独立 px，字号回到正常。
2. **拆掉硬编码 min-width**：知乎顶栏 1175px、问题头部 1032px、主列 694px 的死宽度全部归零。
3. **收拾 emotion 原子 CSS 的漏网之鱼**：按「计算后的实际宽度」扫描全量 DOM，处理显式 px 宽度、`flex-basis`、`flex-shrink:0`、负 margin。
4. **修正 vw/vh**：zoom 下 `100vh` 会被放大 2.49 倍，脚本遍历所有 stylesheet 把 `vw/vh` 折算成 px 重写。

代码块、表格、图片轮播这类**本来就该横向滑动**的容器会打上 `data-zskip` 跳过压缩，保留原生手势。

## 七、更新与国内访问

脚本头部的 `@updateURL` 用的是 `raw.githubusercontent.com`——这是 Tampermonkey 的标准更
新源，但国内部分网络访问不稳定。如果打不开，用下面任一方式：

| 方式 | 地址 |
|---|---|
| jsDelivr CDN（推荐备选） | `https://cdn.jsdelivr.net/gh/leoshone/zhihu-desk2mob@main/zhihu-desk2mob.user.js` |
| **带 commit 的 CDN 地址**（最稳） | `https://cdn.jsdelivr.net/gh/leoshone/zhihu-desk2mob@<完整40位SHA>/zhihu-desk2mob.user.js` |
| 仓库页面手动下载 | `https://github.com/leoshone/zhihu-desk2mob/blob/main/zhihu-desk2mob.user.js` → 点右上角 Raw |

### ⚠ 装完先确认版本号，别信「我装的是最新的」

jsDelivr 的 `@main` 会缓存旧版本，而且**可能一陈就是好几个版本**。0.4.0 推送后实测：

```
@main  →  第 1 次请求返回 @version 0.2.1
         第 2 次请求返回 @version 0.2.0   ← 不同 CDN 节点版本还不一样
```

**raw.githubusercontent.com 的 `@main` 同样有缓存**。0.4.1 推送后实测：GitHub API 确认
main 分支 blob 已是新版（SHA `ef881e6`），但 raw `@main` 连 `?nocache=` 时间戳参数都打
不掉，持续返回 0.4.0。所以「推送成功 ≠ raw 链接立刻是新版」。

也就是说，你以为装的是最新版，实际拿到的是两个版本之前的旧文件。**「装了没变化」有一半是这个原因。**

两个办法：

1. **用带 commit SHA 的地址**（推荐）—— 每个 SHA 对应一份不可变的快照，不存在缓存问题。SHA 在[仓库提交记录](https://github.com/leoshone/zhihu-desk2mob/commits/main)里点 commit 就能看到。
   当前 0.5.0 对应：见[提交记录](https://github.com/leoshone/zhihu-desk2mob/commits/main)最新一条（完整 40 位：`3563dd670e7af036bb67bc04701ca5fd7440aa3a`）。
2. **装完立刻验版本** —— 打开下载到的 js 文件看第 4 行，或者装好后看页面右上角角标，开头就是版本号（`✓ v0.5.1 393px ×2.49`）。

如果 `@main` 拿到的是旧版，可以手动刷新 CDN 缓存（注意是 **GET**，不是 POST）：

```
https://purge.jsdelivr.net/gh/leoshone/zhihu-desk2mob@main/zhihu-desk2mob.user.js
```

浏览器打开这个地址，返回 `"status": "finished"` 就刷新成功了，几分钟内生效。

**仓库**：<https://github.com/leoshone/zhihu-desk2mob>
